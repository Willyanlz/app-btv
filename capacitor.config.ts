import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.labswill.controlebtv',
  appName: 'Controle BTV',
  webDir: 'dist/frontend'
};

export default config;
